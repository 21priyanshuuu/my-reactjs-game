import React from "react";

const Result = ({ score }) => {
  return (
    <div>
      <h1>Your Score: {score}</h1>
      {/* Add any other UI elements here */}
    </div>
  );
};

export default Result;
